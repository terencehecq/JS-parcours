# KEYBOARD

## Les évènements du clavier

Voir les évènements de la souris

```javascript
 let handleKey = (event) => {
    // Faire quelque chose!
};

window.addEventListener ('keydown', handleKey);
```

- keydown
- keypress
- keyup
